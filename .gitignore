scripts/.env
__pycache__/
*.pyc

# Retired 2026-09-03: the PAR data now lives in Cloudflare KV and is served by the Worker
# at par-ordering.acr-ops.com. This repo is a redirect stub only. data.json is ignored so a
# stray refresh run cannot republish 344 wholesale accounts to a PUBLIC GitHub Pages URL,
# which is exactly what it was doing until today.
data.json

# Wrangler's local dev state caches real KV payloads to disk. Two full PAR datasets
# (346 and 343 named wholesale accounts with order history) were committed here and
# served publicly from raw.githubusercontent.com until 2026-09-21 — the 2026-09-03
# retirement only removed the root data.json and missed these entirely.
.wrangler/
node_modules/
